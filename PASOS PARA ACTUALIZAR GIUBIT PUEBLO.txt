📌 CMD paso a paso para subir tus cambios

Entra a la carpeta del proyecto:

cd "D:\GL STUDIOS\SISTEMA PUEBLOVIEJO\super-sorteo-puebloviejo-ec"


Agregar los cambios al “staging area”:

git add .


(el . significa que agrega todos los archivos modificados).

Hacer el commit con un mensaje (puedes poner lo que quieras en el mensaje):

git commit -m "Actualización de códigos"


Subir al repositorio remoto en GitHub:

git push origin main




pasa para ejecutar cmd

cd /d "C:\Users\pc\Documents\SISTEMA\GOLPE DE SUERTE\GOLPEDESUERTE.EC"
py -m venv venv
call venv\Scripts\activate
python -m pip install --upgrade pip
if exist requirements.txt (pip install -r requirements.txt) else (pip install flask)
set DATA_DIR=%CD%\DATA
if not exist "%DATA_DIR%" mkdir "%DATA_DIR%"
python app.py



cd /d "D:\Documents\SISTEMA\GOLPE DE SUERTE\GOLPEDESUERTE.EC" && py -m pip install --upgrade pip && py -m pip install pandas qrcode[pil] pillow flask openpyxl reportlab && py app.py
